🔧 LOGIN NOT WORKING - TROUBLESHOOTING GUIDE

═══════════════════════════════════════════════════════════

STEP 1: CHECK FIREBASE AUTHENTICATION IS ENABLED
═══════════════════════════════════════════════════════════

1. Go to: https://console.firebase.google.com/project/sourov-job-task-projects/authentication/providers

2. Check if "Email/Password" is ENABLED
   - Should show "Enabled" status
   - If not, click on it and toggle "Enable" to ON

3. Click "Save"

═══════════════════════════════════════════════════════════

STEP 2: CHECK BROWSER CONSOLE FOR ERRORS
═══════════════════════════════════════════════════════════

1. Open browser console (F12 or Right-click → Inspect → Console)

2. Go to login page: http://localhost:3001/login

3. Try to sign up with:
   Email: test@test.com
   Password: test123456

4. Look for errors in console. Common errors:

ERROR: "Firebase: Error (auth/operation-not-allowed)"
FIX: Email/Password authentication not enabled in Firebase Console

ERROR: "Firebase: Error (auth/invalid-api-key)"
FIX: Check NEXT_PUBLIC_FIREBASE_API_KEY in frontend/.env.local

ERROR: "Network Error" or "CORS error"
FIX: Backend not running or CORS not configured

ERROR: "Firebase: Error (auth/app-deleted)"
FIX: Firebase project doesn't exist or wrong project ID

═══════════════════════════════════════════════════════════

STEP 3: VERIFY ENVIRONMENT VARIABLES
═══════════════════════════════════════════════════════════

Check frontend/.env.local has these variables:

NEXT_PUBLIC_API_URL=https://gadget-backend-api.vercel.app
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyDe47-j-9lvbbah3qnZHIC3C0nMkWRWTfs
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=sourov-job-task-projects.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=sourov-job-task-projects
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=sourov-job-task-projects.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=466005833276
NEXT_PUBLIC_FIREBASE_APP_ID=1:466005833276:web:2c9067b2c4bcf2b51bb539

If any are missing or wrong, update and restart frontend:
- Stop frontend (Ctrl+C)
- Run: npm run dev

═══════════════════════════════════════════════════════════

STEP 4: CHECK BACKEND IS RUNNING
═══════════════════════════════════════════════════════════

Test backend health:
curl http://localhost:5000/api/health

Should return:
{
  "success": true,
  "message": "Server is running",
  "timestamp": "..."
}

If not working:
- Check backend process is running
- Check backend/.env has correct values
- Restart backend

═══════════════════════════════════════════════════════════

STEP 5: TEST FIREBASE DIRECTLY
═══════════════════════════════════════════════════════════

Open browser console on login page and run:

// Test Firebase config
console.log('Firebase API Key:', process.env.NEXT_PUBLIC_FIREBASE_API_KEY);

// Should show your API key
// If undefined, environment variables not loaded

═══════════════════════════════════════════════════════════

STEP 6: COMMON ISSUES & FIXES
═══════════════════════════════════════════════════════════

ISSUE: "Authentication failed. Please try again."
POSSIBLE CAUSES:
1. Firebase Email/Password not enabled
2. Wrong Firebase credentials
3. Backend not responding
4. CORS error

FIX:
1. Enable Email/Password in Firebase Console
2. Verify all environment variables
3. Check backend is running
4. Check browser console for specific error

---

ISSUE: Button shows "Processing..." forever
POSSIBLE CAUSES:
1. Backend not responding
2. Network error
3. CORS blocking request

FIX:
1. Check backend is running: curl http://localhost:5000/api/health
2. Check browser console for errors
3. Verify NEXT_PUBLIC_API_URL is correct

---

ISSUE: "No account found with this email"
POSSIBLE CAUSES:
1. Trying to login but account doesn't exist
2. Need to sign up first

FIX:
1. Click "Don't have an account? Sign up"
2. Create account first
3. Then try logging in

---

ISSUE: Logout button not showing after login
POSSIBLE CAUSES:
1. Firebase auth state not updating
2. Navbar not re-rendering
3. Browser cache

FIX:
1. Hard refresh (Ctrl+Shift+R)
2. Clear browser cache
3. Check console for "Firebase auth state changed" log
4. Look for 🟢 or 🔴 indicator in navbar

═══════════════════════════════════════════════════════════

STEP 7: MANUAL TEST PROCEDURE
═══════════════════════════════════════════════════════════

1. Open http://localhost:3001/login
2. Open browser console (F12)
3. Click "Don't have an account? Sign up"
4. Enter:
   Email: mytest@example.com
   Password: password123
5. Click "Sign Up"
6. Watch console for logs:
   - Should see Firebase auth logs
   - Should see "Firebase auth state changed: Logged in"
   - Should see "User authenticated: mytest@example.com"
7. Should redirect to /items page
8. Check navbar:
   - Should see 🟢 (green dot)
   - Should see "Logout" button
   - Should see "Add Item" link

═══════════════════════════════════════════════════════════

STEP 8: IF STILL NOT WORKING
═══════════════════════════════════════════════════════════

1. Copy the EXACT error message from browser console
2. Check which step fails:
   - Firebase signup/login?
   - Backend JWT creation?
   - Redirect?
   - Navbar update?

3. Share the error message for specific help

═══════════════════════════════════════════════════════════

QUICK CHECKLIST
═══════════════════════════════════════════════════════════

□ Firebase Email/Password authentication is ENABLED
□ All environment variables in frontend/.env.local are correct
□ Backend is running (test with curl)
□ Frontend is running on port 3001
□ Browser console shows no errors
□ Hard refresh done (Ctrl+Shift+R)
□ Tried creating NEW account (not logging in to non-existent account)

═══════════════════════════════════════════════════════════

What specific error are you seeing? Check browser console!
